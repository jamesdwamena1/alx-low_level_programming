#include "main.h"

/**
  *mul - Multipies two integers
  *@x : the first number to multiply
  *@y: the second to multiply with first number
  * Return: the result of the multiplication of x and y
  */
int mul(int x, int y)
{
	return(x * y);
}
